# 작업 로그 · 2026-07-12

## TL;DR

- UI: 상세화면 출연진 사진 크롭 개선 (세로 카드로 변경)
- Week 5 구현: 로그인 + 즐겨찾기 서버 동기화 인프라 완성
- Apple Sign In → Google OAuth 로 피봇 (유료 개발자 멤버십 회피)
- Google OAuth 코드/문서 완료. **Supabase 대시보드 Google Provider 활성화** 만 하면 실제 로그인 가능
- 최종 상태: `xcodebuild build` 통과 · 경고 0

---

## 1. UI 개선 — 출연진 사진

**문제**: `Circle().frame(72×72)` + `scaledToFill` 에서 TMDB 세로 프로필(2:3) 이 중앙 크롭돼 정수리 + 턱이 잘림.

**해결 과정**:
1. `Circle()` 유지하고 위쪽 정렬(`.top`) + 사이즈 축소 시도 → 여전히 크롭 발생
2. 정사각형(`RoundedRectangle`) 으로 변경 → 여전히 세로 이미지가 잘림
3. **최종**: 프레임 자체를 세로 2:3 (80×120) 으로 맞추고 `scaledToFit` 사용 → 크롭 0

파일: `DramaApp/Features/DramaDetail/DramaDetailView.swift`

---

## 2. Week 5 · 로그인 + 즐겨찾기 동기화

### 처음 시도 (Apple Sign In) — 롤백 결정
Sign in with Apple 로 구현 완료 (`SignInWithAppleButton`, entitlement, nonce/SHA256, ASAuthorization 델리게이트) 후 시뮬레이터 테스트에서 **error 1000 (`AuthorizationError.unknown`)** 발생.

원인 분리: (1) `CODE_SIGN_ENTITLEMENTS` 가 pbxproj 에 누락 → 수동 추가 → (2) 여전히 실패. Sign in with Apple 은 **App ID 에 capability 등록 필요 → 유료 Apple Developer Program 필수** 라는 결론.

**결정**: 유료 멤버십 없이도 되는 Google OAuth 로 피봇. Apple 코드/설정은 롤백:
- ProfileView 에서 `SignInWithAppleButton` 제거 → 플레이스홀더
- 하지만 `AuthStore` / `SupabaseAuthClient` 골격은 그대로 유지 (Google 도 동일 세션 모델)
- `.entitlements` 는 빈 dict 로 남겨서 pbxproj 참조가 유효하게 유지 (Xcode 가 자동 재추가하는 걸 방지)

### Google OAuth (최종 채택)
방식: **ASWebAuthenticationSession + Supabase PKCE OAuth**
- 외부 SDK 의존성 0 (Apple 표준 API 만)
- pbxproj 편집 없음
- 유료 개발자 멤버십 불필요
- 시뮬레이터에서 바로 테스트 가능

### 전체 흐름

```
[마이 탭] "Google 로 계속하기"
     ↓
[OAuthFlow] PKCE code_verifier / challenge 생성 + Supabase authorize URL 조립
     ↓
[ASWebAuthenticationSession] 앱 내부 Safari 시트
     ↓  (사용자가 Google 계정 선택)
[Google → Supabase → 앱]  dramaapp://auth-callback?code=<pkce_code>
     ↓
[SupabaseAuthClient.exchangePKCE] POST /auth/v1/token?grant_type=pkce
     ↓
[AuthStore] AuthSession 저장 (Keychain: access/refresh/userId/expiresAt)
     ↓
[migration 004 트리거] auth.users insert → public.users 자동 브릿지
     ↓
[FavoritesService.syncFromServer] 로컬 → 서버 병합 (로컬 SOT 유지)
```

### 즐겨찾기 동기화 규칙
- **미로그인**: SwiftData 로컬만 (기존 동작 유지)
- **로그인 시**: 로컬 write → 서버 mirror (실패해도 UX 안 막음)
- **로그인 이벤트**: `RootView.onChange(deps.authStore.isSignedIn)` → `syncFromServer` 호출
- **앱 재시작**: `RootView.task` 에서 세션 복원 + `syncFromServer`
- **로그아웃**: Keychain 클리어, 로컬 즐겨찾기 유지

---

## 3. 신규 파일

| 경로 | 역할 |
|---|---|
| `supabase/migrations/004_auth_bridge.sql` | `auth.users` → `public.users` 자동 삽입 트리거 + 제약 완화 |
| `DramaApp/Data/Auth/KeychainStore.swift` | Supabase 세션 토큰 Keychain 보관 |
| `DramaApp/Data/Auth/SupabaseAuthClient.swift` | `/auth/v1/token` (id_token, pkce, refresh) + logout |
| `DramaApp/Data/Auth/OAuthFlow.swift` | PKCE + ASWebAuthenticationSession 오케스트레이션 |
| `DramaApp/Data/Auth/AuthStore.swift` | `@Observable @MainActor` 세션 상태 + Google 로그인 |
| `DramaApp/Data/Repositories/FavoritesService.swift` | 로컬(SwiftData) + 원격(Supabase) 이중 저장 파사드 + 양방향 sync |
| `DramaApp/DramaApp.entitlements` | 빈 dict (pbxproj 참조 유지용) |
| `WEEK5_SETUP.md` | 전체 Week 5 세팅 문서 (Apple 시절 남긴 부분 포함) |
| `WEEK5_GOOGLE_SETUP.md` | Google Cloud Console + Supabase Google Provider 설정 상세 |

## 4. 수정 파일

| 경로 | 변경 |
|---|---|
| `DramaApp/Data/Network/SupabaseHTTPClient.swift` | `apiKey` + `tokenProvider` 로 재작성. POST/DELETE + `Prefer` 헤더 지원 |
| `DramaApp/Data/Network/SupabaseConfig.swift` | `oauthRedirectURL` / `oauthCallbackScheme` 상수 |
| `DramaApp/Info.plist` | `CFBundleURLSchemes: [dramaapp]` (OAuth 콜백용) |
| `DramaApp/App/AppDependencies.swift` | `@MainActor` + `authStore`/`favoritesService` 주입 + `.preview()` 팩토리 |
| `DramaApp/App/RootView.swift` | 앱 부팅/로그인 이벤트 시 `syncFromServer` 자동 트리거 |
| `DramaApp/Features/Profile/ProfileView.swift` | Google 로그인 버튼 (파란색) + 로그아웃 |
| `DramaApp/Features/Schedule/ScheduleView.swift` | 즐겨찾기 토글 → `FavoritesService` 경유 |
| `DramaApp/Features/DramaDetail/DramaDetailView.swift` | 위와 동일 + 출연진 사진 세로 카드 |
| `DramaApp/Features/Favorites/FavoritesView.swift` | 삭제 스와이프 → `FavoritesService` 경유 |
| `DramaApp/DramaApp.xcodeproj/project.pbxproj` | `CODE_SIGN_ENTITLEMENTS` 항목 (빈 entitlements 파일 참조) |

---

## 5. 남은 이슈 (blocker)

### Supabase 대시보드 · Google Provider 미활성화
현재 앱 로그인 시:
```
{"code":400,"error_code":"validation_failed","msg":"Unsupported provider: provider is not enabled"}
```

**조치 순서**:
1. Google Cloud Console → OAuth Consent Screen + Web OAuth Client ID/Secret 발급
2. Supabase 대시보드 → **Authentication → Providers → Google** Enable + Client ID/Secret 입력
3. Supabase 대시보드 → **Authentication → URL Configuration → Redirect URLs** 에 `dramaapp://auth-callback` 추가
4. `supabase/migrations/004_auth_bridge.sql` 실행 (아직이면)

상세는 [`WEEK5_GOOGLE_SETUP.md`](./WEEK5_GOOGLE_SETUP.md).

---

## 6. 다음 스텝 (사용자 결정 대기)

- [ ] 위 대시보드 세팅 → 로그인 성공 확인 → 서버 favorites 테이블에 mirror 검증
- [ ] Week 6: 푸시 알림 (APNs, Supabase Edge Function 로 방영 10분 전 트리거)
- [ ] (선택) Naver 로그인 — Supabase 미지원이라 커스텀 Edge Function 필요

---

## 7. 빌드 상태

```
xcodebuild -project DramaApp.xcodeproj -scheme DramaApp \
  -destination 'platform=iOS Simulator,name=iPhone 17' build
→ ** BUILD SUCCEEDED **  (경고 0)
```

SourceKit 이 synchronized folder 의 새 파일들을 인덱싱하지 못해 Xcode UI 에서
"Cannot find type X in scope" 진단이 뜨지만, 실제 컴파일과 무관.
`⌘⇧K` (Clean Build Folder) 로 해소됨.
