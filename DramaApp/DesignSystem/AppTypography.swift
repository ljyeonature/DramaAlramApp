import SwiftUI

enum AppTypography {
    static let title = Font.system(.title2, weight: .bold)
    static let body = Font.system(.body, weight: .regular)
    static let caption = Font.system(.caption, weight: .regular)
    static let timeMono = Font.system(.callout, design: .monospaced)
    static let badge = Font.system(.caption2, weight: .semibold)
}
