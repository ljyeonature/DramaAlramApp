# 작업 로그 · 2026-07-19

## TL;DR

- 로그아웃 정책 개선: 계정 소유 즐겨찾기만 제거, 게스트 데이터 보존
- **Week 6 로컬 알림 완성** — APNs 미사용, `UNUserNotificationCenter` 만으로 방영 10분 전 알림
- 방영시간 크롤러 정확도 이슈 해결 — 드라마별 오버라이드 필드 + Naver 자동 추출
- 전편 공개 OTT (Netflix/티빙) 편성표에서 하나로 묶음
- 커밋 5건 · 빌드 통과 · 파서 유닛테스트 12/12

---

## 1. 즐겨찾기 소유자 개념 (`d8492c5`)

### 문제
로그인 상태에서 추가한 즐겨찾기가 로그아웃 후에도 로컬(SwiftData)에 남음. 사용자 관점: "내 계정 데이터인데 왜 남지?"

### 해결
`FavoriteDrama.ownerUserId: String?` 필드 도입.

| 시점 | `ownerUserId` | 로그아웃 시 |
|---|---|---|
| 게스트로 추가 | `nil` | ✅ 유지 |
| 로그인 후 추가 | `userId` | ❌ 삭제 |
| 서버에서 sync | `userId` | ❌ 삭제 |

- `FavoritesService.addLocal` — 현재 세션 userId 로 마킹
- `FavoritesService.clearOwnedFavorites(in:)` — `owner != nil` 만 삭제
- `RootView.onChange(isSignedIn)` — 로그아웃 트랜지션에서 자동 호출
- SwiftData 옵셔널 필드 추가는 lightweight migration 자동 처리

---

## 2. Week 6 · 로컬 알림 완성 (`ae4aae4`, `5d1fe71`)

### 왜 로컬 알림?
Apple Sign In 과 마찬가지로 **APNs 도 유료 Apple Developer Program 필수**.
로컬 스케줄링(`UNCalendarNotificationTrigger`)은 무료 · 오프라인 · 시뮬레이터 지원.

### 구성

```
[앱 실행 / 로그인 / 즐겨찾기 토글]
     ↓
[NotificationScheduler.schedule(for: favorite)]
     ↓
[repository.upcomingEpisodes(dramaId, from=now, until=now+7d)]
     ↓
[episodes.groupedByDay() — 전편공개 대응]
     ↓
[각 그룹마다 UNCalendarNotificationTrigger 등록 (airTime - 10min)]
     ↓
[iOS 시스템이 예약 시각에 자동 발송]
```

### 신규 파일

| 경로 | 역할 |
|---|---|
| `DramaApp/Data/Notifications/NotificationScheduler.swift` | 예약/취소/재정합 + 디버그 헬퍼 |
| `DramaApp/Data/Notifications/NotificationDelegate.swift` | 포그라운드에서도 배너 노출 (`willPresent → .banner/.sound/.list`) |

### 수정 파일
- `App/DramaAppApp.swift` — `init` 에서 델리게이트 등록
- `App/AppDependencies.swift` — `notificationScheduler` 주입
- `App/RootView.swift` — 앱 시작 · 로그인 변경 시 `reconcile(favorites:)` 트리거
- `Data/Repositories/DramaRepository.swift` — `upcomingEpisodes(dramaId:from:until:)` 추가 (Mock + Supabase 모두)
- `Data/Repositories/FavoritesService.swift` — 즐겨찾기 토글 시 스케줄러 호출
- `Features/Profile/ProfileView.swift` — 알림 권한 상태 + "허용하기" / "설정 열기" 버튼 + 디버그 버튼 2개
- `Features/DramaDetail/DramaDetailView.swift` — 드라마별 알림 토글

### 설계 결정
- **7일 창** — iOS pending 알림 상한 64개 대응 (즐겨찾기 20개 × 주 2회 = 40)
- **Identifier**: `"drama-{uuid}-ep-{uuid}"` — prefix 매칭으로 드라마 단위 취소
- **Idempotent**: 재정합 시 기존 지우고 다시 등록 → 편성 변경 자동 반영
- **포그라운드 표시**: `UNUserNotificationCenterDelegate.willPresent` 커스텀 응답으로 앱 켜져 있어도 배너 노출

### 디버그 (마이 탭)
- **테스트 알림 (5초 뒤)** — `UNTimeIntervalNotificationTrigger` 로 파이프라인 확인
- **예약된 알림 콘솔 출력** — pending 요청 요약을 Xcode 콘솔에 덤프

---

## 3. 방영시간 오버라이드 스키마 (`5d1fe71`)

### 문제
사용자 리포트: "사랑을 처방해 드립니다" 가 실제 20:00 인데 앱은 22:00 로 표시.

### 원인
크롤러 `airTimes.ts` 는 채널별 하드코딩 슬롯 사용 (KBS/MBC/SBS = 22:00 등).
"사랑을 처방해 드립니다" 는 KBS2 지만 실제 20:00 방영 → 채널 기본값이 부정확.

### migration 005 · `dramas.air_hour_kst / air_minute_kst`

```sql
alter table public.dramas
  add column if not exists air_hour_kst smallint,
  add column if not exists air_minute_kst smallint;
```

- 값 있으면 크롤러가 채널 기본값 대신 사용
- 이미 저장된 `episodes.air_time` 재계산 함수 제공:
  ```sql
  select public.recompute_episode_air_times(<drama_id>);
  ```

### 크롤러 대응
- `airTimes.ts.estimateAirTimeUTC(...)` — 3번째 파라미터로 override 받음
- `mapping.ts.toEpisodeRows(...)` — override 전달
- `supabase.ts.getAirTimeOverride(...)` — 드라마 조회 시 확인
- `index.ts` — episode upsert 전에 override 조회

---

## 4. Naver 방영시간 자동 크롤러 (`4c7b867`)

### 왜?
드라마별 override 세팅을 수동 SQL 로 하나하나 넣는 건 확장성 없음.
TMDB 는 시간 정보 자체를 안 주니 (date only) 다른 소스 필요.
→ **Naver 통합검색** 이 드라마 정보 카드에 방영시간을 텍스트로 노출.

### `crawler/src/naver.ts`
- **fetch** + **plain text 파싱** (cheerio 없이 최소 의존성)
- 2단 전략 (신뢰도 순):
  1. `"방영시간 | 편성시간 | 방송시간"` 앵커 + 근처 300자 시간 표기
  2. 없으면 → `"요일 오전/오후/밤 N시"` 강한 패턴 fallback
- 앵커 우선 → 페이지 상단/광고 오탐 회피
- 실패 시 조용히 null → 채널 기본값 fallback (기존 동작)

### 파서 유닛테스트 12/12 통과
`scratchpad/naver-parser-test.mjs`:
- 오전/오후/밤/저녁 표기
- HH:MM 24시간
- 요일+시간 fallback
- 앵커 우선순위

### 실 Naver 스모크 (4개 드라마)
| 드라마 | 결과 | 매칭 텍스트 |
|---|---|---|
| 사랑을 처방해 드립니다 | 20:00 ✓ | "오후 8시" (앵커) |
| 눈물의 여왕 | 21:10 ✓ | "매주 토·일요일 밤 9시 10분" (fallback) |
| 이번 생도 잘 부탁해 | 21:20 ✓ | "토일 저녁 9시 20분" (fallback) |
| 나의 완벽한 비서 | null | (채널 기본값 사용) |

**Hit rate ~75%.** 실패 케이스는 사용자가 override 로 수동 보정.

### 크롤러 안전장치
- **Idempotent**: 오버라이드가 이미 세팅된 드라마는 Naver 재조회 안 함
- **Rate limit**: 조회당 300ms 대기
- **UA**: 데스크톱 Chrome header 위장

---

## 5. 전편 공개 OTT 그룹핑 (`cc8758b`)

### 문제
Netflix "동궁" 8부작이 편성표에 8줄 반복 표시. 알림도 8개 예약될 상태.

### 원인
크롤러가 TMDB 회차 하나하나 `episodes` 로우로 저장 → 같은 날짜에 같은 시각이 8개.

### 해결
표시 계층에서 **(드라마, KST 하루)** 로 그룹핑.

### 신규 · `DailyScheduleItem`
```swift
struct DailyScheduleItem: Identifiable, Hashable, Sendable {
    let drama: Drama
    let channel: Channel
    let airTime: Date               // 대표 시각 (가장 이른 회)
    let episodeNumbers: [Int]       // 오름차순
    let durationMin: Int

    var episodeLabel: String { ... }  // 상황별 문구
}
```

### `episodeLabel` 로직
- **단일 회차**: `"8회"`
- **배치 + 총 회차 수 일치**: `"전편 공개 (1-8회)"`
- **배치 + 연속**: `"1-8회 공개"`
- **배치 + 비연속**: `"1, 3, 5회"`

### 확장 함수
- `Array<ScheduledEpisode>.groupedByDramaDay() -> [DailyScheduleItem]` — 편성표용
- `Array<Episode>.groupedByDay() -> [(airTime, [Episode])]` — 알림용

### 알림 본문 분기
- 단일: `"8회 방영 10분 전입니다."`
- 배치 연속: `"1-8회 공개 10분 전입니다."`
- 배치 비연속: `"3개 회차 공개 10분 전입니다."`

### 결과
| 케이스 | 편성표 | 알림 |
|---|---|---|
| KBS 미니 (하루 1회) | `21:50 · 8회 · KBS2` | `"8회 방영 10분 전입니다."` |
| Netflix 전편 공개 | `17:00 · 전편 공개 (1-8회) · NETFLIX` | `"1-8회 공개 10분 전입니다."` |
| 지상파 2회 연속 방영 | `21:50 · 1-2회 공개 · MBC` | `"1-2회 공개 10분 전입니다."` |

---

## 6. 사용자 액션 (현재 대기 중)

### Supabase 대시보드
- [ ] `supabase/migrations/005_drama_air_time_override.sql` SQL Editor 에 실행
- (선택) 개별 override:
  ```sql
  update public.dramas
     set air_hour_kst = 20, air_minute_kst = 0
   where title = '사랑을 처방해 드립니다';
  select public.recompute_episode_air_times(
    (select id from public.dramas where title = '사랑을 처방해 드립니다')
  );
  ```

### 크롤러 재실행
```sh
cd crawler && npm run crawl
```
- 오버라이드 없는 드라마는 Naver 자동 조회 시도
- 발견하면 `dramas.air_hour_kst` 에 영속화
- 다음 실행부터는 재조회 스킵

### iOS 앱 확인
1. **편성표** — Netflix 등 OTT 드라마가 한 줄로 압축됐는지
2. **마이 탭** → **알림 허용** 후 **테스트 알림** 버튼
3. 즐겨찾기 토글 → **예약된 알림 콘솔 출력** 으로 pending 확인

---

## 7. 빌드 상태

```
xcodebuild -project DramaApp.xcodeproj -scheme DramaApp \
  -destination 'platform=iOS Simulator,name=iPhone 17' build
→ ** BUILD SUCCEEDED **  (경고 0)

cd crawler && npm run typecheck
→ (통과)

crawler parser tests
→ 12/12 pass, 0 fail
```

---

## 8. 커밋 요약

| 해시 | 스코프 | 설명 |
|---|---|---|
| `d8492c5` | favorites | scope favorites to account, clear on logout |
| `ae4aae4` | week6 | local notifications for airing episodes (T-10min) |
| `5d1fe71` | notifications | foreground banner + per-drama air time override |
| `4c7b867` | crawler | auto-fill drama air time from Naver search |
| `cc8758b` | schedule | collapse binge-release OTT episodes into single row |

---

## 9. 다음 스텝 (선택 대기)

- **원격 푸시(APNs)** — 유료 Apple Developer 확보 시. device_tokens 테이블 + Edge Function 필요
- **알림 딥링크** — 탭 시 해당 드라마 상세로 이동 (`UNNotificationResponse` 처리)
- **뱃지 카운트** — 알림 수신 시 앱 아이콘 뱃지
- **커뮤니티** — 리뷰/평점 (Week 7)
- **위젯** — 오늘 편성표 홈스크린 위젯 (WidgetKit)
- **CI 테스트 확장** — 현재 build 만, `xcodebuild test` 추가
